#!/bin/bash
set -euo pipefail

TPM_DIR="${HOME}/.tmux/plugins/tpm"

if [ ! -d "${TPM_DIR}" ]; then
  echo "Cloning TPM..."
  git clone https://github.com/tmux-plugins/tpm "${TPM_DIR}"
fi

# Install plugins headlessly (works without an attached tmux client)
"${TPM_DIR}/bin/install_plugins" || true
